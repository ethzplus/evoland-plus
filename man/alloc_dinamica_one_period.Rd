% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alloc_dinamica.R
\name{alloc_dinamica_one_period}
\alias{alloc_dinamica_one_period}
\title{Single-period Dinamica EGO allocation}
\usage{
alloc_dinamica_one_period(
  db,
  id_period_post,
  select_score,
  select_maximize,
  work_dir = NULL,
  use_parent_trans_pot = FALSE,
  force_predict_trans_pot = FALSE
)
}
\arguments{
\item{db}{An \link{evoland_db} instance; uses its active \code{id_run}.}

\item{id_period_post}{Integer posterior period ID.}

\item{select_score}{Character; mlr3 measure ID for model selection.}

\item{select_maximize}{Logical; whether to maximise \code{select_score}.}

\item{work_dir}{Character or NULL; directory for Dinamica's input and output files. If
\code{NULL} (default), a temporary directory is used and removed afterwards; a directory
passed explicitly is kept.}

\item{use_parent_trans_pot}{Logical; if TRUE, use the parent run's transition
potentials. Useful if a run branches off from parent.}

\item{force_predict_trans_pot}{Logical; if TRUE, recompute transition potentials even if
\code{trans_pot_t} already holds them for this run and period.}
}
\value{
An \link{lulc_data_t} with the simulated posterior LULC. Nothing is committed:
the caller commits it to \code{lulc_data_t} and, if later periods follow, refreshes the
neighbour predictors with \code{db$upsert_new_neighbors()}.
}
\description{
Allocate LULC changes for a single period using Dinamica EGO, see \link{alloc_dinamica}. The
counterpart of \code{\link[=alloc_clumpy_one_period]{alloc_clumpy_one_period()}}, with the same handling of transition
potentials: existing \code{trans_pot_t} values for the run and period are reused, so they can
be edited between prediction and allocation.
}
