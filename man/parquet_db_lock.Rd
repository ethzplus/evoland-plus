% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet_db_lock.R
\name{parquet_db_lock}
\alias{parquet_db_lock}
\alias{table_lock_path}
\alias{acquire_table_lock}
\alias{release_table_lock}
\title{Cross-process table locking for \link{parquet_db}}
\usage{
table_lock_path(db_path, table_name)

acquire_table_lock(lock_path, timeout = 3600, poll_interval = 0.05)

release_table_lock(lock_path)
}
\arguments{
\item{db_path}{Character path to the database folder}

\item{table_name}{Character table name}

\item{lock_path}{Character path of the lock directory, see \code{\link[=table_lock_path]{table_lock_path()}}}

\item{timeout}{Numeric seconds to wait before erroring out}

\item{poll_interval}{Numeric seconds between the first retries; grows up to
one second as waiting continues}
}
\description{
A minimal, dependency-free advisory lock used to serialise the
read-modify-write cycles that \link{parquet_db} performs on a table's parquet
file(s). Upserts (and non-partitioned appends and deletes) read the existing
data, merge, and rewrite; without a lock two processes doing that
concurrently silently lose each other's rows.

The lock is a directory next to the table file, e.g.
\verb{<db path>/.trans_pot_t.lock}. \code{dir.create()} maps onto a single
\code{mkdir(2)} (\code{CreateDirectory()} on Windows), so exactly one process can win
the race, and it does not rely on advisory file locking, which network file
systems implement unevenly.

Locks are per table and per process:
\itemize{
\item Waiters poll with a jittered backoff until the lock is free or
\code{timeout} seconds have passed, at which point they error.
\item Acquisition is reentrant. A lock already held by the calling process only
bumps a counter, so nesting (e.g. \verb{$commit()} inside a
\verb{$with_table_lock()} block) does not deadlock.
\item A process that dies while holding a lock leaves the directory behind. On
Linux the recorded owner is checked via \verb{/proc} and a lock whose owner is
gone is reclaimed; elsewhere the timeout message points at the directory
to remove.
}
}
\section{Functions}{
\itemize{
\item \code{table_lock_path()}: Path of the lock directory guarding \code{table_name}

\item \code{acquire_table_lock()}: Block until the lock at \code{lock_path} is held by
this process. Returns \code{TRUE} if the lock was newly taken, \code{FALSE} if this
process already held it (reentrant acquisition).

\item \code{release_table_lock()}: Release a lock held by this process. Returns
\code{TRUE} if the lock directory was removed, \code{FALSE} if an outer acquisition
still holds it or this process never held it.

}}
\keyword{internal}
