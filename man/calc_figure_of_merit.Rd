% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{calc_figure_of_merit}
\alias{calc_figure_of_merit}
\title{Figure of merit of simulated land use change}
\usage{
calc_figure_of_merit(initial, observed, simulated, by_transition = FALSE)
}
\arguments{
\item{initial, observed}{Initial and observed final land use: integer vectors of class IDs,
aligned cell by cell, or single-layer SpatRasters on the same grid.}

\item{simulated}{Simulated final land use, in the same form. A list of them is treated as an
ensemble: each member is weighted \code{1 / length(simulated)}, so the counts are expected
counts over the ensemble.}

\item{by_transition}{Logical; if \code{TRUE}, report per transition instead of overall.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}}. Overall (\code{by_transition = FALSE}), one row with
\code{hits}, \code{wrong_hits}, \code{misses}, \code{false_alarms}, \code{figure_of_merit}, \code{producers_accuracy}
(\code{hits} over observed change), \code{users_accuracy} (\code{hits} over simulated change) and
\code{figure_of_merit_null}. Per transition, one row per observed or simulated transition with
\code{id_lulc_anterior}, \code{id_lulc_posterior}, the \code{observed} and \code{simulated} cell counts, \code{hits},
\code{figure_of_merit} (\code{hits} over the union of observed and simulated change) and
\code{figure_of_merit_null}. Cells that are \code{NA} in any input are ignored.
}
\description{
Compares simulated against observed change, both starting from the same initial map, with
Pontius' figure of merit (Pontius et al. 2008). Every cell that changed in either map is one
of:
\itemize{
\item \strong{hit}: observed change, simulated as change to the observed class;
\item \strong{wrong hit}: observed change, simulated as change to another class;
\item \strong{miss}: observed change, simulated as persistence;
\item \strong{false alarm}: observed persistence, simulated as change.
}

The figure of merit is \verb{hits / (misses + hits + wrong hits + false alarms)}, the share of the
union of observed and simulated change that the simulation got right. Cells that persisted in
both maps do not enter it, so, unlike overall agreement, it is not inflated by a mostly
unchanging landscape. Its value scales with how much change there is to get right, so compare
it against \code{figure_of_merit_null}, the value expected when the simulated quantity of each
transition is placed at random among the cells of its initial class.
}
\references{
Pontius, R. G., Boersma, W., Castella, J.-C., et al. (2008). Comparing the input, output, and
validation maps for several models of land change. The Annals of Regional Science, 42(1),
11-37. https://doi.org/10.1007/s00168-007-0138-2
}
