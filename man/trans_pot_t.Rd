% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/trans_pot_t.R
\name{trans_pot_t}
\alias{trans_pot_t}
\alias{as_trans_pot_t}
\alias{print.trans_pot_t}
\alias{predict_trans_pot}
\title{Calculate Transition Potential}
\usage{
as_trans_pot_t(x)

\method{print}{trans_pot_t}(x, nrow = 10, ...)

predict_trans_pot(
  self,
  id_period_post,
  select_score,
  select_maximize,
  force = FALSE,
  cluster = NULL,
  parallel_predict = FALSE
)
}
\arguments{
\item{x}{A list or data.frame coercible to a data.table}

\item{nrow}{see \link[data.table:print.data.table]{data.table::print.data.table}}

\item{...}{passed to \link[data.table:print.data.table]{data.table::print.data.table}}

\item{self}{an \link{evoland_db} instance}

\item{id_period_post}{scalar integerish, passed to \code{\link[=pred_data_wide_v]{pred_data_wide_v()}}}

\item{select_score}{character scalar, name of score/measure to identify best fitting model}

\item{select_maximize}{logical scalar, whether to maximize or minimize \code{select_score}}

\item{force}{logical scalar, recompute transitions that already have potentials
stored for this run and period instead of skipping them}

\item{cluster}{An optional cluster object created by \code{\link[parallel:makeCluster]{parallel::makeCluster()}} or
\code{\link[mirai:make_cluster]{mirai::make_cluster()}}. Each worker predicts one transition at a time and
upserts the result itself; the upserts serialise on the \code{trans_pot_t} lock, so
they stay idempotent and no worker can drop another's rows. See
\link{parquet_db_lock}.}

\item{parallel_predict}{Logical; split each transition's prediction across
workers by setting \code{parallel_predict} on the mlr3 learner. This parallelises
\emph{within} a transition, which helps when there are fewer viable transitions
than cores. It requires an active \code{\link[future:plan]{future::plan()}} — mlr3 dispatches
\code{parallel_predict} through \code{future.apply} and gates it on
\code{\link[future:nbrOfWorkers]{future::nbrOfWorkers()}}, so mirai daemons alone do not enable it; use
\code{future::plan("multisession")}, or \code{future.mirai::mirai_multisession} to run
that future backend on mirai.

The split costs one model serialisation per chunk, so it only pays off for
expensive learners over many rows. Measured on 4 workers: \code{classif.rpart} at
50k rows was 5x \emph{slower}, \code{classif.ranger} (500 trees) at 50k rows 1.6x
slower, and the same learner at 500k rows 2.6x faster. Benchmark before
enabling it.}
}
\value{
A data.table of class "trans_pot_t" with columns:
\itemize{
\item \code{id_trans}: Foreign key to \code{\link[=trans_meta_t]{trans_meta_t()}}
\item \code{id_period_post}: Foreign key to \code{\link[=periods_t]{periods_t()}}
\item \code{id_coord}: Foreign key to \code{\link[=coords_t]{coords_t()}}
\item \code{value}: Map of model (hyper) parameters
}

\code{predict_trans_pot()}: called for side effect; commit \code{trans_pot_t} to
database. Invisibly returns a data.table with one row per transition reporting
how many rows were written.
}
\description{
Estimate transition potential at \code{id_period_post}. Based on the LULC at \code{id_period_anterior}
}
\section{Methods (by generic)}{
\itemize{
\item \code{print(trans_pot_t)}: Print a trans_pot_t object, passing params to data.table print

}}
\section{Functions}{
\itemize{
\item \code{predict_trans_pot()}: For each viable transition in current \code{id_run}, predict the raw
transition potential for a given period and store it in \code{trans_pot_t} in the database. Raw
potentials are per-transition MLR3 model probabilities; they are \strong{not} yet allocation-ready
(not column-scaled to target rates, not row-closed to max probability of 1). Use
\code{\link[=adjusted_trans_pot_v]{adjusted_trans_pot_v()}} to obtain allocation-ready values.

}}
