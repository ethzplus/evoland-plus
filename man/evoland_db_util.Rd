% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evoland_db_util.R
\name{evoland_db_util}
\alias{evoland_db_util}
\alias{get_evoland_db_read_expr}
\alias{run_parallel_evoland}
\title{Utility functions and methods for \code{evoland_db}}
\usage{
get_evoland_db_read_expr(self, table_name)

run_parallel_evoland(items, worker_fun, parent_db, cluster = NULL, ...)
}
\arguments{
\item{self}{The \code{evoland_db} instance}

\item{table_name}{The name of the table to read from}

\item{items}{A list of items to iterate over}

\item{worker_fun}{A function to apply to each item, with signature \code{worker_fun(item, db, ...)}. The \code{db} argument will be a new \code{evoland_db} instance for each worker.}

\item{parent_db}{An \link{evoland_db} instance; used for its \code{path} and \code{id_run}; or
passed directly to the worker for serial case.}

\item{cluster}{An optional cluster object created by \code{\link[parallel:makeCluster]{parallel::makeCluster()}} or
\code{\link[mirai:make_cluster]{mirai::make_cluster()}}.}

\item{...}{Additional arguments passed to \code{worker_fun}.}
}
\value{
A SQL expression string to read most specific data for slice

A list of results
}
\description{
Factored out utility functions and methods specifically used in conjunction with
\code{evoland_db} and its subclasses.
}
\section{Functions}{
\itemize{
\item \code{get_evoland_db_read_expr()}: Gets the read expression for a given table, taking into
account the run lineage and distinctness columns. If the table has no \code{id_run} column
or is the \code{runs_t} table itself, falls back to the default read expression.
Otherwise, constructs a read expression that filters for the active \code{id_run} and its lineage,
using the distinctness columns to determine which slices of data to include.

\item \code{run_parallel_evoland()}: Run a worker function in parallel with an \code{evoland_db}
instance; takes an (optional) worker cluster, a worker function, a set of
items, and a parent \code{evoland_db} instance. If \strong{no} cluster is passed, apply the
worker serially. \strong{If} a cluster is passed, each worker gets its own database
instance derived from the parent database.

}}
