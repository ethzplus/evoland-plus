% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{figure_of_merit_v}
\alias{figure_of_merit_v}
\title{Figure of merit of simulated land use change}
\usage{
figure_of_merit_v(
  self,
  id_period_anterior,
  id_period_post,
  id_run_reference,
  id_run_simulated = self$id_run,
  by_transition = FALSE,
  exclude_id_lulc = integer(0)
)
}
\arguments{
\item{self}{An \link{evoland_db} instance}

\item{id_period_anterior}{Integer, period of the initial map}

\item{id_period_post}{Integer, period of the observed and simulated maps}

\item{id_run_reference}{Integer, run holding the initial and observed maps}

\item{id_run_simulated}{Integer vector, runs holding the simulated maps; defaults to the
active run}

\item{by_transition}{Logical; if \code{TRUE}, report per transition instead of overall.}

\item{exclude_id_lulc}{Integer vector of classes; cells whose initial or observed class is
one of them are ignored. Default none.}
}
\value{
A \code{\link[data.table:data.table]{data.table::data.table()}} with one row per simulated \code{id_run} (and transition).
Overall (\code{by_transition = FALSE}): \code{hits}, \code{wrong_hits}, \code{misses}, \code{false_alarms},
\code{figure_of_merit}, \code{producers_accuracy} (\code{hits} over observed change), \code{users_accuracy}
(\code{hits} over simulated change) and \code{figure_of_merit_null}. Per transition:
\code{id_lulc_anterior}, \code{id_lulc_posterior}, the \code{observed} and \code{simulated} cell counts,
\code{hits}, \code{figure_of_merit} (\code{hits} over the union of observed and simulated change) and
\code{figure_of_merit_null}. Cells missing from any of the three maps are ignored.
}
\description{
Compares simulated against observed change with Pontius' figure of merit (Pontius et al.
2008). The initial and observed maps are read from a reference run, the simulated maps from
one or more other runs, each through its own run lineage. Every cell that changed in either
the observed or a simulated map is one of:
\itemize{
\item \strong{hit}: observed change, simulated as change to the observed class;
\item \strong{wrong hit}: observed change, simulated as change to another class;
\item \strong{miss}: observed change, simulated as persistence;
\item \strong{false alarm}: observed persistence, simulated as change.
}

The figure of merit is \verb{hits / (misses + hits + wrong hits + false alarms)}, the share of the
union of observed and simulated change that the simulation got right. Cells that persisted in
both maps do not enter it, so, unlike overall agreement, it is not inflated by a mostly
unchanging landscape. Its value scales with how much change there is to get right, so compare
it against \code{figure_of_merit_null}, the value expected when the simulated quantity of each
transition is placed at random among the cells of its initial class.

Cells whose change is not the model's to get right, such as a class whose transitions are
imposed deterministically, can be left out with \code{exclude_id_lulc}: a cell is ignored if its
initial or its observed class is one of them.

For an ensemble of runs, the expected counts are the means of the per-run counts; recompute
the ratios from those means rather than averaging the per-run ratios.
}
\references{
Pontius, R. G., Boersma, W., Castella, J.-C., et al. (2008). Comparing the input, output, and
validation maps for several models of land change. The Annals of Regional Science, 42(1),
11-37. https://doi.org/10.1007/s00168-007-0138-2
}
\keyword{internal}
