% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet_db_utils.R
\name{parquet_db_utils}
\alias{parquet_db_utils}
\alias{as_parquet_db_t}
\alias{resolve_cols}
\alias{resolve_partition_clause}
\alias{resolve_metadata_clause}
\alias{convert_list_cols}
\alias{list_to_kv_df}
\alias{kv_df_to_list}
\alias{create_table_binding}
\alias{create_method_binding}
\alias{cols_to_select_expr}
\title{Parquet database utility functions}
\usage{
as_parquet_db_t(
  x,
  class_name = character(),
  key_cols = NULL,
  alternate_key_cols = NULL,
  map_cols = NULL,
  partition_cols = NULL
)

resolve_cols(x, metadata = list(), attr = character(1))

resolve_partition_clause(x, metadata = list())

resolve_metadata_clause(x, metadata = list())

convert_list_cols(x, cols, fn)

list_to_kv_df(x)

kv_df_to_list(x)

create_table_binding(table_name, mode = c("write_once", "upsert", "append"))

create_method_binding(fun, with_private = FALSE, with_super = FALSE)

cols_to_select_expr(cols, table_name)
}
\arguments{
\item{x}{The data to be committed.}

\item{class_name}{Optional class name to prepend before "parquet_db_t". Used
to create more specific subclasses.}

\item{key_cols}{Optional character vector. Used for data.table's \link[data.table:setkey]{data.table::setkey}
and used by \link{parquet_db} to determine which columns to use for upsert
operations. Together with \code{alternate_key_cols}, these are used to define
uniqueness constraints.}

\item{alternate_key_cols}{Optional character vector. These are used in
conjunction with \code{key_cols} to define uniqueness constraints, but are
not used for upsert operations. Example: numeric primary key whose
distinctness is identical to a character primary key column, but
upsert operations should be based on the character key, leaving the
character<->numeric mapping intact.}

\item{map_cols}{Optional character vector. Used to coerce columns to DuckDB's
MAP type, used to store named unnested lists from R.}

\item{partition_cols}{Optional character vector. Used to specify columns for
hive style parquet file partitioning.}

\item{metadata}{The metadata to be committed, as returned by \link{parquet_db} \verb{$get_table_metadata()}}

\item{attr}{The column function to resolve, e.g. "alternate_key_cols", "map_cols", "key_cols".}

\item{cols}{The columns to select.}

\item{fn}{The function to apply to each element of the list columns}

\item{table_name}{The name of the table to bind to.}

\item{fun}{The underlying function to bind as an R6 method, which must have a \code{self} argument}

\item{with_private}{Whether to also pass the R6 reference to \code{private} as an argument}

\item{with_super}{Whether to also pass the R6 reference to \code{super} as an argument}
}
\description{
A set of utility functions for working with \link{parquet_db}.
}
\section{Functions}{
\itemize{
\item \code{as_parquet_db_t()}: Coerce to parquet_db_t subclass. It coerces an object to a
data.table and adds attributes that \link{parquet_db} relies on for database-like
operations. See the paramaters for details.

\item \code{resolve_cols()}: Resolves which columns to use for key, map,
partition, etc.; pre-existing schema takes precedence.

\item \code{resolve_partition_clause()}: Compose partitioning clause.

\item \code{resolve_metadata_clause()}: Compose metadata clause, which includes any new
metadata from the data's attributes, as well as any existing metadata from
the database; existing metadata cannot be safely overwritten. New metadata
takes precedence over existing metadata for this commit. Any atomic vector is
coerced to a quoted comma separated list of values, which are recovered in
\link{parquet_db} \verb{$get_table_metadata()}. Non-atomic metadata values are dropped
with a warning.

\item \code{convert_list_cols()}: Convert list columns by applying \code{fn}

\item \code{list_to_kv_df()}: Convert a named list to a data.frame with "key" and
"value" columns.

\item \code{kv_df_to_list()}: Convert a data.frame with "key" and "value" columns to a
named list.

\item \code{create_table_binding()}: Create a binding function for a table, which can be used
to fetch or commit data to that table. The active binding either returns the table
(missing argument), or upserts to it (assignment operation)

\item \code{create_method_binding()}: Helper function to bind a function as a method to an R6
generator. Simply passes the R6 method's arguments as-is to the underlying "pure"
function, passing the R6 reference to \code{self}, but not to \code{private}.

\item \code{cols_to_select_expr()}: Paste vector of escaped column names into a SQL
select statement, with optional table name prefix.

}}
\keyword{internal}
